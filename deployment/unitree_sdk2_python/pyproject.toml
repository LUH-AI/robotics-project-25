[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"


[project]
name = "unitree-sdk2py"
version = "0.1"
requires-python = "== 3.9.19"
dynamic = [
    "description",
    "readme",
    "license",
    "authors",
    # "dependencies",
    "urls",
]
dependencies = ["cyclonedds==0.10.2", "numpy", "opencv-python", "pygame"]
